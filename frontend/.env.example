# Local dev points the Vite app at the backend dev server.
# In production behind Caddy, leave this empty/unset so the app uses same-origin /api.
VITE_API_URL=http://localhost:3000
# Ko-fi username — dejar vacío para ocultar el botón de donación
VITE_KOFI_USERNAME=dnthdev
